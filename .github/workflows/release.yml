name: Release Windows Key Only
on:
  push:
    tags:
      - "v*"

jobs:
  release:
    permissions:
      contents: write
    runs-on: windows-latest
    defaults:
      run:
        shell: pwsh

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Install Rust stable
        uses: dtolnay/rust-toolchain@stable

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20

      - name: Install dependencies
        run: npm install

      # DECODIFICAR Y EXTRAER SOLO LA SEGUNDA LÍNEA (LA CLAVE)
      - name: Decode and Write Key
        run: |
          $KeyFile = "$PWD\tauri.key"
          $Base64Content = "${{ secrets.TAURI_SIGNING_PRIVATE_KEY }}"

          # Decodificar Base64
          $Bytes = [System.Convert]::FromBase64String($Base64Content)
          $KeyText = [System.Text.Encoding]::UTF8.GetString($Bytes)

          # Separar por líneas y tomar SOLO la segunda (la clave real, sin comentario)
          $Lines = $KeyText -split "`n"
          if ($Lines.Count -ge 2) {
              $RawKey = $Lines[1].Trim()
              Write-Host "✅ Extrayendo solo la clave (línea 2, sin comentario)"
          } else {
              throw "❌ El secreto no tiene el formato esperado"
          }

          # Escribir SOLO la clave
          $Utf8NoBom = New-Object System.Text.UTF8Encoding $false
          [System.IO.File]::WriteAllText($KeyFile, $RawKey, $Utf8NoBom)

          Write-Host "Primeros 10 chars: $($RawKey.Substring(0, 10))..."

      # COMPILAR
      - name: Build Tauri App
        run: npm run tauri build

      # FIRMAR Y GENERAR JSON
      - name: Sign and Generate latest.json
        run: |
          $MsiFile = Get-ChildItem -Path "src-tauri/target/release/bundle/msi/*.msi" | Select-Object -First 1
          if (-not $MsiFile) { throw "No se encontró el MSI" }

          # Firmar usando -f (file path)
          $SignOutput = npm run tauri signer sign -- -f "tauri.key" "$($MsiFile.FullName)" 2>&1 | Out-String
          Write-Host "Output Firma: $SignOutput"

          $SigFile = "$($MsiFile.FullName).sig"
          if (Test-Path $SigFile) {
             $Signature = Get-Content $SigFile
             Write-Host "✅ Firma generada"
          } else {
             if ($SignOutput -match "([a-zA-Z0-9+/]{60,}=*)") {
                $Signature = $matches[1]
                Write-Host "✅ Firma del output"
             } else {
                throw "❌ ERROR: No se pudo generar firma"
             }
          }

          # JSON
          $Version = "${{ github.ref_name }}".TrimStart('v')
          $PubDate = (Get-Date).ToUniversalTime().ToString("yyyy-MM-ddTHH:mm:ssZ")
          $DownloadUrl = "https://github.com/${{ github.repository }}/releases/download/v$Version/$($MsiFile.Name)"

          $JsonContent = @"
          {
            "version": "$Version",
            "notes": "Update v$Version",
            "pub_date": "$PubDate",
            "platforms": {
              "windows-x86_64": {
                "signature": "$Signature",
                "url": "$DownloadUrl"
              }
            }
          }
          "@

          $JsonPath = Join-Path (Get-Location) "latest.json"
          Set-Content -Path $JsonPath -Value $JsonContent -Encoding UTF8
          Write-Host "✅ latest.json creado"

      - name: Upload Release Assets
        uses: softprops/action-gh-release@v1
        if: startsWith(github.ref, 'refs/tags/v')
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        with:
          files: |
            src-tauri/target/release/bundle/msi/*.msi
            src-tauri/target/release/bundle/nsis/*.exe
            latest.json
